[package]
name = "cli"
version = "1.0.0"
edition = "2024"

[dependencies]
generic-array.workspace = true
anyhow.workspace = true
pngfier-core = { path = "../core" }
clap = { version = "4.6.6", features = ["derive"] }
